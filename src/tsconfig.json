{
  "extends": ["@tsconfig/strictest/tsconfig.json"],
  "compilerOptions": {
    "target": "ESNext",
    "module": "preserve",
    "lib": ["ESNext", "ESNext.Array", "ESNext.AsyncIterable", "DOM", "DOM.Iterable", "DOM.AsyncIterable", "webworker"]
  }
}
